{
  "name": "anti-bullying-game-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "A privacy-first Phaser + TypeScript anti-bullying bystander practice MVP for ages 8-12.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && npm run test && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "vite": "^8.3.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
